"use strict";

module.exports = {
  // Preserve existing asset values. Region corrections must be performed
  // explicitly through the application instead of mutating production data
  // as part of a schema migration.
  async up() {},

  async down() {},
};
